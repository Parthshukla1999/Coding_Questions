class Solution{
  public:
    int missingNumber(vector<int>& a, int n) {
        // Your code goes here
       sort(a.begin(),a.end());
       for(int i = 0 ; i<=n-1;i++){
          if(a[i]==i+1)
          continue;
          else
          return i+1;
       }
       return n;
    }
};
